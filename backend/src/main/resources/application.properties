# ==================== DATABASE (Neon PostgreSQL) ====================
spring.datasource.url=jdbc:postgresql://ep-sweet-truth-apoyjucu-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_2NrUSBYhLGd0
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ==================== JWT ====================
app.jwt.secret=${JWT_SECRET:clubborrow-super-secret-key-minimum-256-bits-for-hs256-2024}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# ==================== MAIL (Gmail SMTP) ====================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# ==================== SERVER ====================
server.port=${PORT:8081}

# ==================== APP ====================
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,https://your-netlify-app.netlify.app}
app.from-email=${MAIL_USERNAME:noreply@clubborrow.com}
app.from-name=Hệ Thống Mượn Đồ CLB

# ==================== LOGGING ====================
logging.level.com.btl.clubborrow=INFO
